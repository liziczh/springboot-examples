server:
  port: ${jvm_port:80}
  servlet:
    context-path: /easyexcel-demo

spring:
  application:
    name: easyexcel-demo-web
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# log
logging:
  config: classpath:logback-spring.xml